{% extends 'base.html.twig' %}

{% block title %}Lexique{% endblock %}

{% block body %}
    <h1>Lexique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lexique.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ lexique.Nom }}</td>
            </tr>
            <tr>
                <th>Definition</th>
                <td>{{ lexique.Definition }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ lexique.Categorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lexique_index') }}">back to list</a>

    <a href="{{ path('app_lexique_edit', {'id': lexique.id}) }}">edit</a>

    {{ include('lexique/_delete_form.html.twig') }}
{% endblock %}
